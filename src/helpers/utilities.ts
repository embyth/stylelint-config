import type { Config } from 'stylelint';

export function defineConfig(
  config: Config,
): Config {
  return config;
}

export function defineRules(
  rules: Config['rules'],
): Config['rules'] {
  return rules;
}
